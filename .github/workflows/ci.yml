name: CypressAPI

on:
  push:
    branches:
      - main

jobs:
  cypress-run:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Instalar dependências
        run: npm ci

      - name: Executar testes Cypress
        uses: cypress-io/github-action@v6
        with:
          build: npm ci
          start: npx cypress run

      - name: Notificar no Discord
        if: always()
        run: |
          STATUS="${{ job.status }}"
          curl -H "Content-Type: application/json" \
               -X POST \
               -d "{\"content\": \"🚨 *Workflow CypressAPI finalizado com status:* **${STATUS}**\"}" \
               https://discord.com/api/webhooks/1388966990130970714/4rlfI4pG2nFA6snod_8P1-XBheQjwzU35ajPzj2YfAUpKk6ZJbaDUdkmZtIsEyC_iulM
